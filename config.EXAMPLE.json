{
  // Discord bot token.
  "token": "",

  // Sentry DSN URL for catching errors.
  "sentryDSN": "",

  // Feedback Discord webhook.
  "feedbackWebhook": {
    "id": "",
    "token": ""
  },

  // Google Assistant settings.
  "googleAssistantSettings": {
    // Google Assistant OAuth authentication.
    "auth": {
      // Full path to the Google Assistant credentials file.
      "keyFilePath": "/home/wicopee/glowing-memory/googleAssistantCredentials.json",
      // Full path to the Google Assistant token storage file.
      "savedTokensPath": "/home/wicopee/glowing-memory/googleAssistantTokens.json"
    },

    // Google Assistant Conversation settings.
    "conversation": {
      "lang": "en-US",               // GA Conversation language.
      "screen": { "isOn": false },   // GA Conversation flag for screen data (recommended to keep it as is).

      // USE THIS ONLY IF YOU WENT THROUGH DEVICE REGISTRATION
      // PROCESS.
      "deviceModelId": "glowing-memory-device",
      "deviceId": "glowmem",

      // Device location.
      "deviceLocation": {
        "coordinates": {
          "latitude": 37.422070403830226,
          "longitude": -122.08408713606059
        }
      }
    }
  },

  "formatCredentials": {
    "youtube": {
      // IPv6 Block
      "ipv6": ""
    }
  }
}